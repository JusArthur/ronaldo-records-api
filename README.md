# ronaldo-records-api

This is a simple RESTful API built with Node.js and Express that provides information about Cristiano Ronaldo's football records. 
The API serves data in JSON format and allows users to retrieve various records held by the football legend.